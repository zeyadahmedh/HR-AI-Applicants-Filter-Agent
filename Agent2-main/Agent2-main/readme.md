# Job Application Portal with Qdrant Storage

A standalone job application portal that stores candidate applications in Qdrant vector database. The HR CV Filter agent will read directly from Qdrant.

## Architecture
